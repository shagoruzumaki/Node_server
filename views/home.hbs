<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
      background: #f3f4f6;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }
    .box {
      background: white;
      padding: 2.5rem 3rem;
      border-radius: 12px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
      text-align: center;
      max-width: 400px;
    }
    h1 {
      margin: 0 0 0.7rem;
      font-size: 2rem;
      color: #111827;
    }
    p {
      margin: 0;
      color: #6b7280;
    }
    .btn {
      margin-top: 1.5rem;
      display: inline-block;
      background: #2563eb;
      color: white;
      padding: 0.6rem 1.3rem;
      border-radius: 6px;
      text-decoration: none;
      font-size: 0.95rem;
      font-weight: 600;
      transition: background .15s;
    }
    .btn:hover { background: #1e40af; }
  </style>
</head>
<body>
  <div class="box">
    <h1> {{msg}} </h1>
    <p>You made it. The server is running correctly.</p>
    <a href="#" class="btn">Get Started</a>
  </div>
</body>
<footer>
    <p>Copyright @{{Year}}</p>
</footer>
</html>
